services:
  # FastSaaS host-port convention: standard service port + 100. Host-only;
  # container-internal ports are unchanged. Keeps several SaaS-stack projects
  # coexisting on one workstation.
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: fastsaas
      POSTGRES_PASSWORD: dev
      POSTGRES_DB: fastsaas
    ports:
      - "5532:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./infra/postgres/init:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U fastsaas -d fastsaas"]
      interval: 2s
      timeout: 3s
      retries: 20

  redis:
    image: redis:7-alpine
    ports:
      - "6479:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 2s
      timeout: 3s
      retries: 20

  mailhog:
    image: mailhog/mailhog:latest
    ports:
      - "1125:1025"   # SMTP
      - "8125:8025"   # Web UI

volumes:
  postgres_data:
  redis_data:
